version: "3.8"

services:
  finance-tracker:
    build:
      context: .
      dockerfile: Containerfile
    container_name: finance-tracker
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
